# Starting instructions
* Install PHP
* Download the source code by either .zip or just clone this .git repository
* Execute the "project.sql"
* Run application

## Connections
* PDO
* PostgreSQL

## Requirements
* PHP 8 >= version
* Enabled PDO PGSQL driver on PHP.ini
